#pragma once
#include <iostream>
#include <vector>
#include <string>

namespace VectorUtil {

	void putIfAbsent(std::vector<char>* vec, char c);

	std::string valuesToCommaString(std::vector<char> vec);

}
